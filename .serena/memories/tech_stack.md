# Tech Stack

- Rust package `jakarta-backend`, edition 2024, binary target named `jakarta-backend` at root `main.rs`.
- No dependencies declared at onboarding time.
- Intended runtime from project instructions: Cloudflare Workers. Worker-specific crates/config are not yet present.