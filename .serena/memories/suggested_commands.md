# Suggested Commands

- `cargo check` - compile check the Rust binary.
- `cargo test` - run tests once tests exist.
- `cargo fmt` - format Rust files.
- `cargo clippy --all-targets --all-features` - lint once dependencies/tooling support it.
- `serena memories check` - validate memory references from repo root.