# Task Completion

- Run `cargo fmt` after Rust edits.
- Run `cargo check` for compile validation.
- Run `cargo test` when tests are present or behavior changes include test additions.
- For Cloudflare Worker config changes, validate with Wrangler once `wrangler.toml`/Worker toolchain exists.